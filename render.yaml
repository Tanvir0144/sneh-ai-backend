services:
  # ----------------- Database Service -----------------
  - type: pserv
    name: sneh-ai-db
    runtime: postgres
    plan: free

  # ----------------- Web Service -----------------
  - type: web
    name: sneh-ai-backend
    env: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sneh-ai-db
      - key: SECRET_KEY
        generateValue: true
      - key: ALGORITHM
        value: "HS256"
